# TODO: Исправление недочетов проекта WiChess

## 🔴 Критические проблемы безопасности (Приоритет 1)

### 1. Добавить JWT middleware для валидации токенов

-   [+] Создать файл `backend/src/middleware/auth.middleware.ts`
-   [+] Реализовать функцию `verifyToken` в `JwtService`
-   [+] Создать middleware для проверки JWT токенов
-   [+] Применить middleware к защищенным роутам (`/api/users`)
-   [+] Добавить проверку на undefined для `JWT_SECRET_KEY`

**Файлы для изменения:**

-   `backend/src/jwt/jwt.service.ts` - добавить метод `verifyToken(token: string)`
-   `backend/src/middleware/auth.middleware.ts` - создать новый файл
-   `backend/src/users/users.module.ts` - применить middleware

### 2. Исправить JWT сервис

-   [+] Добавить проверку на существование `JWT_SECRET_KEY` в конструкторе
-   [+] Добавить `expiresIn` при создании токенов (например, '24h')
-   [+] Реализовать метод `verifyToken` для проверки токенов
-   [+] Удалить неиспользуемое поле `uuid` из класса

**Файлы для изменения:**

-   `backend/src/jwt/jwt.service.ts`

### 3. Улучшить безопасность хранения токенов

-   [+] Рассмотреть использование httpOnly cookies вместо localStorage
-   [+] Добавить refresh токены (опционально, но рекомендуется)
-   [ ] Реализовать механизм отзыва токенов (blacklist)

**Файлы для изменения:**

-   `backend/src/auth/auth.service.ts` - добавить refresh токены
-   `frontend/src/store/store.ts` - изменить способ хранения
-   `frontend/src/api/api.config.ts` - настроить работу с cookies

### 4. Добавить валидацию входных данных

-   [+] Установить библиотеку валидации (например, `zod` или `class-validator`)
-   [+] Создать схемы валидации для email и password
-   [+] Добавить валидацию email формата на бэкенде
-   [+] Добавить проверку сложности пароля (минимум 8 символов, буквы и цифры)
-   [+] Добавить ограничения на длину полей (email max 255, password min 8, max 128)

**Файлы для изменения:**

-   `backend/src/auth/auth.controller.ts` - добавить валидацию перед обработкой
-   Создать `backend/src/validators/auth.validator.ts` - схемы валидации

---

## 🟠 Архитектурные проблемы (Приоритет 2)

### 5. Исправить типизацию PORT

-   [+] Изменить тип `PORT` на `number | string`
-   [+] Добавить преобразование `process.env.SERVER_PORT` в число
-   [+] Добавить значение по умолчанию (например, 3000)
-   [+] Добавить валидацию порта (должен быть в диапазоне 1-65535)

**Файлы для изменения:**

-   `backend/src/app/app.service.ts`

### 6. Исправить конфликт модулей TypeScript

-   [ ] Изменить `"module": "commonjs"` на `"module": "ES2020"` или `"ESNext"` в `tsconfig.json`
-   [ ] Или изменить `"type": "module"` на `"type": "commonjs"` в `package.json` (если нужен CommonJS)
-   [ ] Проверить, что все импорты работают корректно после изменений
-   [ ] Обновить скрипты в `package.json` если необходимо

**Файлы для изменения:**

-   `backend/tsconfig.json`
-   `backend/package.json` (возможно)

### 7. Улучшить обработку ошибок

-   [ ] Создать централизованный error handler middleware
-   [ ] Создать кастомные классы ошибок (например, `ValidationError`, `AuthError`)
-   [ ] Заменить `console.log` на proper logging (например, `winston` или `pino`)
-   [ ] Добавить обработку ошибок Prisma
-   [ ] Улучшить сообщения об ошибках (не раскрывать внутренние детали)

**Файлы для изменения:**

-   Создать `backend/src/middleware/error.middleware.ts`
-   Создать `backend/src/utils/errors.ts` - кастомные классы ошибок
-   `backend/src/app/app.service.ts` - подключить error middleware
-   Все контроллеры - использовать кастомные ошибки

### 8. Исправить HTTP статусы

-   [ ] В `auth.controller.ts`: возвращать 401 при ошибке логина, 400 при валидации
-   [ ] В `auth.service.ts`: не возвращать `error` в объекте успешного ответа
-   [ ] Убедиться, что все ошибки возвращают правильные статусы

**Файлы для изменения:**

-   `backend/src/auth/auth.controller.ts`
-   `backend/src/auth/auth.service.ts`

---

## 🟡 Проблемы с базой данных (Приоритет 3)

### 9. Улучшить схему Prisma

-   [ ] Добавить индексы где необходимо
-   [ ] Добавить валидацию на уровне Prisma (если возможно)
-   [ ] Рассмотреть добавление полей `createdAt` и `updatedAt`
-   [ ] Добавить поле `refreshToken` в модель User (если используем refresh токены)

**Файлы для изменения:**

-   `backend/prisma/schema.prisma`
-   Создать миграцию после изменений

### 10. Подготовка к production

-   [ ] Рассмотреть переход с SQLite на PostgreSQL или MySQL
-   [ ] Настроить переменные окружения для разных сред (dev, prod)
-   [ ] Добавить стратегию бэкапов БД

---

## 🟢 Проблемы на фронтенде (Приоритет 3)

### 11. Убрать дублирование логики аутентификации

-   [ ] Использовать только Zustand store для проверки аутентификации
-   [ ] Убрать прямые обращения к `localStorage.getItem('token')` из других мест
-   [ ] Создать единый источник правды для состояния аутентификации

**Файлы для изменения:**

-   `frontend/src/store/store.ts`
-   `frontend/src/api/api.config.ts` - использовать store вместо прямого localStorage

### 12. Добавить обработку ошибок API

-   [ ] Добавить interceptor для обработки ошибок в `api.config.ts`
-   [ ] Обрабатывать 401 ошибки (автоматический logout)
-   [ ] Показывать пользователю понятные сообщения об ошибках
-   [ ] Добавить обработку network errors

**Файлы для изменения:**

-   `frontend/src/api/api.config.ts` - добавить response interceptor
-   `frontend/src/api/api.service.ts` - добавить обработку ошибок

### 13. Использовать переменные окружения

-   [ ] Создать `.env` файл для фронтенда (или `.env.example`)
-   [ ] Заменить хардкод `baseURL` на переменную окружения
-   [ ] Настроить Vite для работы с env переменными

**Файлы для изменения:**

-   `frontend/src/api/api.config.ts`
-   Создать `frontend/.env.example`

### 14. Исправить CORS настройки

-   [ ] Вынести origin в переменную окружения
-   [ ] Поддержать несколько origins для разных сред

**Файлы для изменения:**

-   `backend/src/app/app.service.ts`
-   Создать `backend/.env.example`

---

## 🔵 Улучшение кода (Приоритет 4)

### 15. Удалить неиспользуемый код

-   [ ] Удалить пустые методы `getUser` и `removeUser` из `users.controller.ts`
-   [ ] Или реализовать эти методы, если они нужны
-   [ ] Удалить неиспользуемое поле `uuid` из `JwtService`

**Файлы для изменения:**

-   `backend/src/users/users.controller.ts`
-   `backend/src/jwt/jwt.service.ts`

### 16. Исправить грамматические ошибки

-   [ ] "The user does not exists" → "The user does not exist"
-   [ ] "Passwords does not equals" → "Passwords do not match"
-   [ ] "some bissines logic" → исправить опечатку и добавить реальную логику

**Файлы для изменения:**

-   `backend/src/auth/auth.service.ts`

### 17. Реализовать logout

-   [ ] Добавить реальную логику logout (например, добавление токена в blacklist)
-   [ ] Или просто удалить токен на фронтенде (если не нужен server-side logout)

**Файлы для изменения:**

-   `backend/src/auth/auth.service.ts`
-   `frontend/src/store/store.ts` - добавить метод logout

### 18. Добавить валидацию env переменных

-   [ ] Создать файл `backend/src/config/env.config.ts`
-   [ ] Проверять наличие всех обязательных переменных при старте
-   [ ] Выбрасывать понятную ошибку, если переменные отсутствуют
-   [ ] Создать `.env.example` с примерами всех переменных

**Файлы для изменения:**

-   Создать `backend/src/config/env.config.ts`
-   `backend/src/index.ts` - добавить проверку при старте
-   Создать `backend/.env.example`

---

## 🟣 Производительность и безопасность (Приоритет 5)

### 19. Добавить rate limiting

-   [ ] Установить библиотеку `express-rate-limit`
-   [ ] Настроить rate limiting для auth endpoints (например, 5 попыток в 15 минут)
-   [ ] Настроить общий rate limiting для всех endpoints

**Файлы для изменения:**

-   `backend/src/app/app.service.ts`
-   `backend/src/auth/auth.module.ts` - добавить rate limiting middleware

### 20. Добавить кэширование (опционально)

-   [ ] Рассмотреть кэширование часто запрашиваемых данных
-   [ ] Использовать Redis или in-memory cache для пользователей

---

## 📝 Дополнительные улучшения

### 21. Добавить логирование

-   [ ] Установить библиотеку логирования (winston, pino)
-   [ ] Настроить разные уровни логирования для dev и prod
-   [ ] Логировать важные события (логин, регистрация, ошибки)

**Файлы для изменения:**

-   Создать `backend/src/utils/logger.ts`
-   Все сервисы и контроллеры

### 22. Добавить тесты

-   [ ] Настроить тестовое окружение (Jest или Vitest)
-   [ ] Написать unit тесты для сервисов
-   [ ] Написать integration тесты для API endpoints

### 23. Улучшить документацию

-   [ ] Обновить README с инструкциями по настройке
-   [ ] Добавить описание API endpoints
-   [ ] Добавить примеры запросов

---

## 📋 Порядок выполнения (рекомендуемый)

1. **День 1-2: Критические проблемы безопасности**

    - Задачи 1, 2, 4 (JWT middleware, валидация)

2. **День 3: Архитектурные исправления**

    - Задачи 5, 6, 7, 8 (PORT, TypeScript, ошибки, статусы)

3. **День 4: Фронтенд и окружение**

    - Задачи 11, 12, 13, 14, 18 (фронтенд, env переменные)

4. **День 5: Улучшение кода**

    - Задачи 15, 16, 17 (удаление кода, грамматика, logout)

5. **День 6+: Дополнительные улучшения**
    - Задачи 19, 20, 21, 22, 23 (rate limiting, логирование, тесты)

---

## ⚠️ Важные замечания

-   Перед началом работы создайте новую ветку в git
-   Делайте коммиты после каждого выполненного пункта
-   Тестируйте изменения после каждого исправления
-   Не забывайте обновлять `.env.example` файлы
-   Документируйте изменения в коде

---

## 📌 Быстрая справка по командам

```bash
# Backend
cd backend
npm install
npm run dev

# Frontend
cd frontend
npm install
npm run dev

# Prisma
cd backend
npx prisma generate
npx prisma migrate dev
npx prisma studio
```

---
