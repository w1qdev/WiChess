# ♟️ WiChess - Онлайн Шахматная Платформа

> Современная веб-платформа для игры в шахматы с системой аутентификации и управления пользователями

## 📋 Содержание

-   [О проекте](#-о-проекте)
-   [Текущие возможности](#-текущие-возможности)
-   [Технологии](#-технологии)
-   [Начало работы](#-начало-работы)
-   [Установка](#-установка)
-   [Конфигурация](#-конфигурация)
-   [API Документация](#-api-документация)
-   [Структура проекта](#-структура-проекта)
-   [Безопасность](#-безопасность)
-   [Разработка](#-разработка)
-   [TODO](#-todo)

---

## 🎯 О проекте

WiChess - это веб-платформа для игры в шахматы, находящаяся в активной разработке. На текущем этапе реализована базовая система аутентификации и управления пользователями с современным стеком технологий.

### Текущий статус

✅ **Реализовано:**

-   Система регистрации и авторизации
-   JWT аутентификация с refresh токенами
-   Защита API endpoints
-   Валидация и санитизация данных
-   Логирование событий
-   Rate limiting
-   Безопасные HTTP заголовки

🚧 **В разработке:**

-   Игровая логика шахмат
-   WebSocket для real-time игры
-   Система рейтингов
-   История партий

---

## ✨ Текущие возможности

### Аутентификация

-   🔐 **Регистрация пользователей** - создание аккаунта с валидацией данных
-   🔑 **Авторизация** - вход в систему с JWT токенами
-   🚪 **Выход из системы** - безопасный logout
-   🔄 **Refresh токены** - автоматическое обновление access токенов
-   🛡️ **Защита роутов** - JWT middleware для приватных endpoints

### Безопасность

-   🔒 **Хеширование паролей** - bcrypt с 10 раундами
-   ✅ **Валидация данных** - Zod схемы для входных данных
-   🧹 **Санитизация** - очистка пользовательского ввода
-   ⏱️ **Rate Limiting** - защита от брутфорса и DDoS
-   🪖 **Helmet** - безопасные HTTP заголовки
-   🌐 **CORS** - настройка кросс-доменных запросов

### Управление пользователями

-   👥 **Список пользователей** - получение всех пользователей (защищенный endpoint)
-   📧 **Поиск по email** - проверка существования пользователя
-   📝 **Логирование** - отслеживание важных событий

---

## 🛠️ Технологии

### Frontend

-   **React 19** - UI библиотека
-   **TypeScript** - типизированный JavaScript
-   **Vite** - быстрый сборщик
-   **Tailwind CSS 4** - утилитарный CSS фреймворк
-   **Zustand** - легковесное управление состоянием
-   **Axios** - HTTP клиент
-   **React Router 7** - маршрутизация
-   **React Hot Toast** - уведомления
-   **Framer Motion** - анимации
-   **Ant Design** - UI компоненты
-   **Lucide React** - иконки

### Backend

-   **Node.js** - серверная среда выполнения
-   **Express 5** - веб-фреймворк
-   **TypeScript** - типизированный JavaScript
-   **Prisma** - ORM для работы с БД
-   **SQLite** - база данных (для разработки)
-   **JWT** - аутентификация (jsonwebtoken)
-   **Bcrypt** - хеширование паролей
-   **Zod** - валидация схем
-   **Winston** - логирование
-   **Helmet** - безопасные HTTP заголовки
-   **express-rate-limit** - защита от перегрузки
-   **CORS** - кросс-доменные запросы

### DevOps & Инструменты

-   **Git** - контроль версий
-   **ESLint** - линтер для кода
-   **TypeScript** - статическая типизация
-   **Prisma Migrate** - миграции базы данных

---

## 🚀 Начало работы

### Предварительные требования

Убедитесь, что у вас установлено:

-   **Node.js** (версия 18.0.0 или выше)
-   **npm** (версия 9.0.0 или выше)
-   **Git**

```bash
# Проверка версий
node --version
npm --version
git --version
```

---

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/wichess.git
cd wichess
```

### 2. Установка зависимостей

#### Backend

```bash
cd backend
npm install
```

#### Frontend

```bash
cd ../frontend
npm install
```

### 3. Настройка базы данных

```bash
cd backend
npx prisma generate
npx prisma migrate dev
```

Это создаст SQLite базу данных в файле `backend/dev.db`.

### 4. Настройка переменных окружения

#### Backend (.env)

Создайте файл `.env` в папке `backend/`:

```env
# Порт сервера
SERVER_PORT=3000

# URL клиента
CLIENT_URL=http://localhost:5173

# JWT секретный ключ (обязательно!)
JWT_SECRET_KEY=your_super_secret_jwt_key_minimum_32_characters_long

# База данных SQLite
DATABASE_URL="file:./dev.db"

# Окружение
NODE_ENV=development
```

**⚠️ Важно:** Сгенерируйте безопасный JWT_SECRET_KEY:

```bash
# Linux/Mac
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Windows PowerShell
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

#### Frontend (.env)

Создайте файл `.env` в папке `frontend/` (опционально, если нужно изменить URL):

```env
VITE_API_URL=http://localhost:3000/api
```

### 5. Запуск приложения

#### Режим разработки

**Терминал 1 - Backend:**

```bash
cd backend
npm run dev
```

Сервер запустится на `http://localhost:3000`

**Терминал 2 - Frontend:**

```bash
cd frontend
npm run dev
```

Приложение откроется в браузере по адресу `http://localhost:5173`

#### Production сборка

**Backend:**

```bash
cd backend
npm run build
npm start
```

**Frontend:**

```bash
cd frontend
npm run build
npm run preview
```

---

## ⚙️ Конфигурация

### Настройка CORS

В файле `backend/src/app/app.service.ts` можно настроить разрешенные origins:

```typescript
cors({
    origin: process.env.CLIENT_URL || 'http://localhost:5173',
    credentials: true,
})
```

### Настройка JWT

Время жизни токенов настраивается в `backend/src/jwt/jwt.service.ts`:

```typescript
// Access token - 15 минут
expiresIn: '15m'

// Refresh token - 7 дней
expiresIn: '7d'
```

### Настройка Rate Limiting

В файле `backend/src/app/app.service.ts`:

```typescript
rateLimit({
    windowMs: 15 * 60 * 1000, // 15 минут
    max: 100, // максимум 100 запросов
})
```

---

## 📚 API Документация

### Базовый URL

```
http://localhost:3000/api
```

### Аутентификация

Все защищенные endpoints требуют JWT токен в заголовке:

```
Authorization: Bearer <access_token>
```

### Endpoints

#### POST `/api/auth/signup`

Регистрация нового пользователя.

**Request Body:**

```json
{
    "email": "user@example.com",
    "password": "SecurePass123!",
    "passwordRepeat": "SecurePass123!"
}
```

**Response (200):**

```json
{
    "success": true,
    "tokens": {
        "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
    }
}
```

**Response (400):**

```json
{
    "error": "Неверный Email" // или "Неверный Пароль", "Пароли не совпадают"
}
```

#### POST `/api/auth/login`

Вход в систему.

**Request Body:**

```json
{
    "email": "user@example.com",
    "password": "SecurePass123!"
}
```

**Response (200):**

```json
{
    "success": true,
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**Response (400/401):**

```json
{
  "error": "Email и пароль являются обязательными"
}
// или
{
  "error": "Этот пользователь не существует"
}
```

#### POST `/api/auth/logout`

Выход из системы.

**Request Body:**

```json
{
    "email": "user@example.com"
}
```

**Response (200):**

```json
{
    "success": true
}
```

#### GET `/api/users`

Получение списка всех пользователей. **Требует аутентификации.**

**Headers:**

```
Authorization: Bearer <access_token>
```

**Response (200):**

```json
[
    {
        "id": "uuid",
        "email": "user@example.com",
        "name": null
    }
]
```

**Response (401):**

```json
{
    "message": "Unauthorized"
}
```

---

## 📁 Структура проекта

```
WiChess/
├── backend/                 # Backend приложение
│   ├── src/
│   │   ├── app/            # Конфигурация Express приложения
│   │   ├── auth/           # Модуль аутентификации
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   └── auth.module.ts
│   │   ├── users/          # Модуль пользователей
│   │   │   ├── users.controller.ts
│   │   │   ├── users.service.ts
│   │   │   └── users.module.ts
│   │   ├── jwt/            # JWT сервис
│   │   ├── hash/           # Хеширование паролей
│   │   ├── middlewares/    # Express middleware
│   │   │   ├── jwt.middleware.ts
│   │   │   └── error.middleware.ts
│   │   ├── prisma/         # Prisma клиент
│   │   ├── utils/          # Утилиты
│   │   │   ├── logger.service.ts
│   │   │   ├── validation.service.ts
│   │   │   └── sanitize.service.ts
│   │   └── index.ts        # Точка входа
│   ├── prisma/
│   │   ├── schema.prisma   # Схема базы данных
│   │   └── migrations/     # Миграции
│   ├── generated/          # Сгенерированный Prisma клиент
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                # Frontend приложение
│   ├── src/
│   │   ├── api/            # API клиент
│   │   │   ├── api.config.ts
│   │   │   └── api.service.ts
│   │   ├── components/     # React компоненты
│   │   │   ├── header/
│   │   │   ├── hero/
│   │   │   └── popups/
│   │   ├── pages/          # Страницы
│   │   │   ├── WelcomePage/
│   │   │   └── HomePage/
│   │   ├── store/          # Zustand store
│   │   │   └── store.ts
│   │   ├── common/         # Общие утилиты
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── package.json
│   └── vite.config.ts
│
├── TODO.md                 # Список задач
└── Readme.md               # Этот файл
```

---

## 🔒 Безопасность

### Реализованные меры безопасности

1. **JWT аутентификация**

    - Access токены (15 минут)
    - Refresh токены (7 дней)
    - Валидация токенов на каждом запросе

2. **Хеширование паролей**

    - Bcrypt с 10 раундами
    - Пароли никогда не хранятся в открытом виде

3. **Валидация и санитизация**

    - Zod схемы для валидации
    - Санитизация пользовательского ввода
    - Защита от SQL injection (Prisma)

4. **Rate Limiting**

    - Общий лимит: 100 запросов за 15 минут
    - Защита от DDoS и брутфорса

5. **HTTP Security Headers**

    - Helmet для безопасных заголовков
    - CORS настройка
    - XSS защита

6. **Логирование**
    - Winston для логирования событий
    - Отслеживание важных операций

### Рекомендации для production

-   [ ] Использовать PostgreSQL вместо SQLite
-   [ ] Настроить HTTPS
-   [ ] Реализовать token blacklist
-   [ ] Добавить более строгий rate limiting для auth endpoints
-   [ ] Настроить мониторинг и алерты
-   [ ] Реализовать audit logging
-   [ ] Использовать Redis для rate limiting
-   [ ] Настроить backup базы данных

---

## 💻 Разработка

### Скрипты

#### Backend

```bash
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка проекта
npm start        # Запуск production версии
```

#### Frontend

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm run preview  # Просмотр production сборки
npm run lint     # Проверка кода линтером
```

### Работа с базой данных

```bash
# Генерация Prisma клиента
npx prisma generate

# Создание миграции
npx prisma migrate dev --name migration_name

# Применение миграций
npx prisma migrate deploy

# Открыть Prisma Studio (GUI для БД)
npx prisma studio
```

### Code Style

Проект использует:

-   **TypeScript** для типизации
-   **ESLint** для проверки кода
-   **Prettier** (рекомендуется) для форматирования

---

## 📝 TODO

См. файл [TODO.md](./TODO.md) для полного списка задач и планов развития проекта.

### Основные направления развития

-   [ ] Реализация игровой логики шахмат
-   [ ] WebSocket для real-time игры
-   [ ] Система рейтингов (ELO)
-   [ ] История партий
-   [ ] Экспорт партий в PGN
-   [ ] Система друзей
-   [ ] Чат во время игры
-   [ ] Анализ партий

---

## 📄 Лицензия

Этот проект находится в разработке. Лицензия будет определена позже.

---

## 👥 Контакты

-   **GitHub**: [yourusername/wichess](https://github.com/yourusername/wichess)
-   **Issues**: [GitHub Issues](https://github.com/yourusername/wichess/issues)

---

**Последнее обновление:** 2025-01-30
